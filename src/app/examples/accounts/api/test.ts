import { Api } from '#lib/context/Api';

export class TestSession extends Api {
  async get() {
    // await this.auth();

    // return {
    //   user: this.user,
    //   session: this.session,
    // };
  }
}
